{
  "schema": 1,
  "name": "socat-rescue",
  "description": "No-residue socat TLS shell; stages socat.",
  "menu_label": "No-residue socat TLS shell; stages socat",
  "risk_level": "high",
  "size_hint": "medium",
  "autorun": "reverse access",
  "network_behavior": "reverse shell when explicitly launched or selected as zero-arg behavior",
  "external_write_behavior": "disabled",
  "validated_cases": [
    "make smoke-test",
    "integration-glinet: socat-rescue"
  ],
  "notes": "Uses best-effort ephemeral runtime cleanup. Requires staged socat and a reachable operator endpoint."
}
