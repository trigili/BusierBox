{
  "schema": 1,
  "name": "ssh-operator",
  "description": "Extract Dropbear/dbclient; explicit reverse SSH; no autorun.",
  "menu_label": "Extract Dropbear/dbclient; explicit reverse SSH; no autorun",
  "risk_level": "medium",
  "size_hint": "medium",
  "autorun": "none",
  "network_behavior": "SSH transport available when explicitly launched",
  "external_write_behavior": "disabled",
  "validated_cases": [
    "make smoke-test",
    "integration-glinet: ssh-operator"
  ],
  "notes": "Stages Dropbear tools but does not modify root authorized_keys unless external-write options are explicitly changed."
}
